<!-- res/drawable/button_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Fondo blanco -->
    <stroke
        android:width="1dp"
        android:color="@color/fixerBlue" /> <!-- Borde de color fixerBlue -->
    <corners android:radius="50dp" /> <!-- Esquinas redondeadas opcionales -->
    <padding
        android:left="24dp"
        android:top="6dp"
        android:right="24dp"
        android:bottom="6dp" />
</shape>
